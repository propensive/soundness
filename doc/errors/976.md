# `SN-976` — `GitRefError`

Raised when a value is not a valid Git reference name.

## Cause

Octogenarian validates reference names against `git
check-ref-format`'s rules. The constructor records the offending
`value` and a `reason: Reason`; the rendered message is `$value
is not a valid Git reference because $reason`.

## Variants

### `SN-976.1` — `LeadingOrTrailingDot`

A path segment of the reference starts or ends with `.`.

**Example.** `[↯SN-976.1] .foo is not a valid Git reference because a path segment starts or ends with .`

**Resolution.** Trim the leading or trailing dot from the segment.

### `SN-976.2` — `ReservedSuffix`

A path segment ends with `.lock`, which Git reserves for lock
files.

**Example.** `[↯SN-976.2] feature.lock is not a valid Git reference because a path segment ends with the reserved suffix .lock`

**Resolution.** Choose a name without the `.lock` suffix.

### `SN-976.3` — `ReservedSequence`

The name contains `@{`, which Git reserves for reflog selectors
(e.g. `HEAD@{1}`).

**Example.** `[↯SN-976.3] foo@{bar} is not a valid Git reference because the name contains the reserved sequence @{`

**Resolution.** Remove or replace the `@{` sequence.

### `SN-976.4` — `DoubleDot`

The name contains `..`, which Git reserves for range selectors.

**Example.** `[↯SN-976.4] foo..bar is not a valid Git reference because the name contains ..`

**Resolution.** Replace the `..` with a single `.` or another
separator.

### `SN-976.5` — `EmptySegment`

The name contains an empty path segment — typically a leading,
trailing, or doubled `/`.

**Example.** `[↯SN-976.5] foo//bar is not a valid Git reference because the name contains an empty path segment`

**Resolution.** Remove the empty segment.

### `SN-976.6` — `InvalidCharacter`

The name contains a character Git forbids in references: any of
`*`, `[`, `\`, ` `, `^`, `~`, `:`, or `?`.

**Example.** `[↯SN-976.6] foo bar is not a valid Git reference because the name contains a character forbidden by Git`

**Resolution.** Replace the forbidden character with `-` or `_`.

### `SN-976.7` — `BadHash`

The value passed to `GitHash` was not a 40-character lowercase
hexadecimal string.

**Example.** `[↯SN-976.7] abc is not a valid Git reference because the value is not a 40-character hexadecimal hash`

**Resolution.** Provide the full 40-character SHA-1 commit hash.

## See also

- Source: `lib/octogenarian/src/core/octogenarian.GitRefError.scala` (line 57)
- `SN-685` — Git operation failure variants
