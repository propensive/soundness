# Machine-readable index of roadmap items. One `item` record per item across the
# nine track files in doc/roadmap/. This file is the single source of status:
# the prose files never claim completion; they link here.
#
# Fields:
#   id         the permanent item identifier (never reused)
#   track      the track file the item belongs to (without .md)
#   horizon    near | near-mid | mid | mid-long | long
#   status     open | done
#   needs      hard dependency, intra- or cross-track (repeated)
#   baseline   measured starting point, with measurement date
#   criterion  the Done-when command or observable fact (authority: the track file)
#   done       the closing pull request and date; present only when status is done
#
# Multi-token values use hard-space mode: a double space after the keyword.

tel 1.0

item
  id core-1
  track core
  horizon near
  status open
  baseline  32 files use sorted/ordered stdlib collections (2026-09-25; 16 on 2026-08-01)
  criterion  git grep -lE 'TreeMap|TreeSet|TrieMap|SortedMap|SortedSet' -- lib | grep -v '^lib/proscenium/' | wc -l == 0

item
  id core-2
  track core
  horizon near-mid
  status done
  done  #1848 (2026-08-24)
  needs core-1
  baseline  475 files import proscenium.compat; the compat file is 551 lines (2026-08-01)
  criterion  git grep -l 'import proscenium.compat' -- lib | wc -l == 0, and the compat file has no members

item
  id core-3
  track core
  horizon near-mid
  status open
  needs core-1
  baseline  228 files import scala.collection, 106 of them scala.collection.mutable (2026-09-25; 219 and 170 on 2026-08-01)
  criterion  git grep -l 'import scala.collection' -- lib | grep -v '^lib/proscenium/' | wc -l == 0

item
  id core-4
  track core
  horizon mid
  status open
  baseline  1606 while loops, 476 readUnchecked, 507 charAt in lib core (2026-09-25; 1597, 482, 509 on 2026-09-14)
  criterion  no collection exposes a partial indexed apply (issue #1666); every remaining while is a shape doc/standards/loops.md sanctions, with a comment naming its invariant

item
  id core-5
  track core
  horizon mid-long
  status open
  criterion  test suite asserts no Java encodings in rendered traces, values and type names

item
  id core-6
  track core
  horizon long
  status open
  criterion  totality audit runs in CI and reports zero violations

item
  id safety-1
  track safety
  horizon near
  status open
  baseline  468 untrackedCaptures occurrences (2026-09-25; 288 on 2026-08-01)
  criterion  git grep -o untrackedCaptures -- lib | wc -l == 0

item
  id safety-2
  track safety
  horizon near
  status open
  baseline  416 unsafeAssumeSeparate, 502 unsafeAssumePure, 50 unsafeErasedValue (2026-09-25; 323, 273, 45 on 2026-08-01)
  criterion  every remaining caps.unsafe occurrence names a rep/ case; script reports zero unannotated

item
  id safety-3
  track safety
  horizon mid
  status open
  criterion  the rep/ capturing-raises reproduction compiles cleanly under the current toolchain

item
  id safety-4
  track safety
  horizon mid-long
  status open
  needs safety-3
  baseline  1436 caps.unsafe occurrences in total (2026-09-25; 929 on 2026-08-01)
  criterion  git grep -oE 'unsafeAssumeSeparate|untrackedCaptures|unsafeAssumePure|unsafeErasedValue' -- lib | wc -l == 0

item
  id safety-5
  track safety
  horizon long
  status open
  criterion  CI consumes published artifacts from a mainline-Scala project; rep/ documents the rebase procedure and the cost of the two most recent rebases

item
  id safety-6
  track safety
  horizon long
  status open
  baseline  87 of 424 modules compile without separation checking: 40 components, 27 test suites, 20 benchmark modules; 2 opt for settings.cc alone (2026-09-25; "3 components" on 2026-08-01 counted only downward overrides)
  criterion  no component in build.mill compiles with anything weaker than settings.sep

item
  id safety-7
  track safety
  horizon near
  status open
  criterion  the census is recorded per release and one per-file gate replaces check-while-count.py and check-stdlib-count.sh

item
  id plat-1
  track platforms
  horizon near
  status open
  baseline  101 of 144 rows build for JavaScript, 35 excluded with reasons; nothing regenerates or checks the file (2026-09-25; 94 of 132 on 2026-08-01)
  criterion  CI regenerates doc/compatibility.tsv, fails on divergence, and no exclusion lacks a reason

item
  id plat-2
  track platforms
  horizon near-mid
  status open
  criterion  make attest includes the Android stage; CI records a signed APK per release

item
  id plat-3
  track platforms
  horizon mid
  status open
  needs plat-1
  criterion  no JavaScript or Native exclusion in the manifest has a non-inherent reason; Native link check covers every Native-capable module

item
  id plat-4
  track platforms
  horizon mid-long
  status open
  baseline  7 WASI backends; HTTP any method; sockets TCP-only (2026-09-25; HTTP was GET-only on 2026-08-01)
  criterion  the manifest WASI column matches the JavaScript column except where the reason names a missing WASI capability

item
  id plat-5
  track platforms
  horizon long
  status open
  needs plat-4
  criterion  every exclusion reason in doc/compatibility.tsv is inherent; a reason-vocabulary check enforces it in CI

item
  id plat-6
  track platforms
  horizon mid
  status open
  criterion  the soundness.dev front-end ships as Soundness-compiled Scala.js with all web API access through generated typed facades, in the ordinary build

item
  id api-1
  track api
  horizon near
  status open
  baseline  4 renamed names still at top level; 61 prefix families with 210 names plus 216 singletons listed (2026-09-25)
  criterion  the rename table in doc/api-nesting-proposal.md is fully applied, and the 'Remaining names by prefix family' section of doc/api-reduction-candidates.md lists no name outside 'Reviewed items which should not be moved'

item
  id api-2
  track api
  horizon near
  status open
  baseline  12 homonym pairs, 1 synonym family; check-given-uniqueness.py checks given names only (2026-09-25)
  criterion  the C4-homonym and C4-synonym sections are empty; a same-name-same-thing check runs in the ordinary build

item
  id api-3
  track api
  horizon mid
  status open
  baseline  3 C2 abbreviations listed (2026-09-25)
  criterion  the C2 section of doc/api-reduction-candidates.md is empty (C3 and C3b were superseded by doc/api-nesting-proposal.md and closed with it)

item
  id api-4
  track api
  horizon mid
  status open
  criterion  naming-conformance sweep reports zero deviations and runs in the ordinary build

item
  id api-5
  track api
  horizon long
  status open
  needs api-1
  needs api-2
  needs api-3
  criterion  doc/api-reduction-candidates.md no longer exists

item
  id api-6
  track api
  horizon long
  status open
  needs dist-2
  criterion  the dist-2 CI check enforced on every surface-changing pull request for ten consecutive releases

item
  id doc-1
  track documentation
  horizon near
  status open
  baseline  40 error types carry no SN code; 241 error pages; no parity script (2026-09-25; 39 and 233 on 2026-08-01)
  criterion  parity check in CI; git grep -hE 'extends +Error\((m"|[a-z])' -- lib | wc -l == 0

item
  id doc-2
  track documentation
  horizon near
  status open
  baseline  1 Stub marker remains (2026-09-25)
  criterion  grep -c 'Stub' doc/standards/messages.md == 0

item
  id doc-3
  track documentation
  horizon near
  status done
  done  #1791 (2026-08-14)
  baseline  92 files of per-module readme boilerplate; 98 lib/*/doc/ directories (2026-08-01)
  criterion  no file matches the legacy readme boilerplate, and no lib/*/doc/ directory remains

item
  id doc-4
  track documentation
  horizon mid
  status open
  baseline  99 topics in doc/modules/; 1 of 144 libraries (burdock) uncovered, and the script exits non-zero (2026-09-25; 91 topics and 35 uncovered on 2026-08-01)
  criterion  python3 etc/check-doc-coverage.py reports 144/144 libraries covered and exits 0

item
  id doc-5
  track documentation
  horizon mid
  status open
  baseline  every published docJar is empty; build.mill stubs the task (2026-09-25, unchanged since 2026-08-01)
  criterion  every published component has non-empty API documentation, published per release

item
  id doc-6
  track documentation
  horizon mid
  status open
  criterion  web/res/content/ contains no hand-maintained duplicate of any doc/ page

item
  id doc-7
  track documentation
  horizon long
  status open
  criterion  link check verifies each philosophy page is referenced from the topic guides of the modules embodying it

item
  id doc-8
  track documentation
  horizon long
  status open
  needs tool-2
  needs tool-4
  criterion  the from-nothing walkthrough is executed by an agent in CI and recorded per release

item
  id tool-1
  track tooling
  horizon near
  status open
  baseline  flame 0.3.0 pinned in etc/tools; current release 0.68.0; no per-release flame build (2026-09-25)
  criterion  flame CI builds against every new release before it is announced

item
  id tool-2
  track tooling
  horizon near-mid
  status open
  baseline  fume 0.4.1 runs the suite in CI; 1 suite disabled: orthodoxy (2026-09-25)
  criterion  fume runs the full suite in CI; grep -c 'enabled = false' build.mill == 0

item
  id tool-3
  track tooling
  horizon near-mid
  status open
  baseline  exegesis's suite drives an in-process server over JSON-RPC: initialize, open, diagnose, hover, change; no completion, navigation or transport (2026-09-25)
  criterion  scripted LSP editor-session test passes in CI

item
  id tool-4
  track tooling
  horizon mid
  status open
  criterion  fury build builds Soundness from a clean checkout, attestation-equal to the Mill build

item
  id tool-5
  track tooling
  horizon mid
  status open
  baseline  synesthesia is the generic MCP library; none of the SN, topic, roadmap or migration resources exists (2026-09-25)
  criterion  scripted MCP session resolves an SN code, a module topic and a roadmap item status, in CI

item
  id tool-6
  track tooling
  horizon mid
  status open
  criterion  probably ships forAll-style generation with shrinking, or a philosophy page documents the axes/spread design

item
  id tool-7
  track tooling
  horizon long
  status open
  needs doc-5
  criterion  fluence answers name, type and signature queries against the current release

item
  id tool-8
  track tooling
  horizon long
  status open
  criterion  a fume run can be watched live in a browser; the git-note report matches the stream

item
  id tool-9
  track tooling
  horizon long
  status open
  baseline  flair, flame and fume read .pyrocosm/<tool>/config.tel (2026-09-25)
  criterion  no ecosystem tool accepts any configuration format but TEL

item
  id tool-10
  track tooling
  horizon near-mid
  status open
  baseline  vivisection ships a JDWP core, evaluator and DAP server with a scripted live-session suite; objects inspect as JVM class names; no topic (2026-09-25)
  criterion  scripted DAP session (breakpoint, step, inspect with Soundness-rendered values) passes against a running application in CI

item
  id tool-11
  track tooling
  horizon mid
  status open
  needs tool-2
  criterion  every CI run records coverage in a git note; one command reports the coverage delta between any two commits

item
  id tool-12
  track tooling
  horizon mid
  status open
  needs tool-2
  criterion  every commit on main carries a benchmark git note; one command reports the benchmark delta between any two commits

item
  id dist-1
  track distribution
  horizon near
  status open
  baseline  no changelog; release_notes.py (propensive/.github) assembles the Changes section from merged pull-request bodies; releases are cut by signed tag (2026-09-25)
  criterion  changelog exists; the shared release (configured by etc/release) fails when the released version has no entry

item
  id dist-2
  track distribution
  horizon near
  status open
  baseline  doc/migration.md and 0.65.0.md–0.68.0.md exist; the migration directive in etc/release gates the release; no pull-request check (2026-09-25)
  criterion  doc/migration.md exists; the release (the migration directive in etc/release) refuses to tag without doc/migration/<version>.md; a CI check fails any pull request that changes lib/**/src without touching doc/migration/pending.md

item
  id dist-3
  track distribution
  horizon near-mid
  status open
  baseline  reliquary reads and assembles .lira files with unit round-trips; no whole-release round-trip (2026-09-25)
  criterion  the LIRA reference implementation round-trips a Soundness release byte-identically in a scripted test

item
  id dist-4
  track distribution
  horizon mid
  status open
  needs doc-5
  criterion  versions are computed from the extracted API surface; an incompatible change forces the version it should

item
  id dist-5
  track distribution
  horizon mid
  status open
  needs tool-4
  criterion  fury publish produces and publishes signed .lira files for a real release

item
  id dist-6
  track distribution
  horizon mid-long
  status open
  criterion  signature, namespace and transparency-log inclusion verified by a single shipped command

item
  id dist-7
  track distribution
  horizon mid
  status open
  needs dist-3
  criterion  a release ships to LIRA, attested; make verify-attest covers the published artifacts

item
  id dist-8
  track distribution
  horizon long
  status open
  needs dist-5
  needs dist-6
  needs dist-7
  criterion  the release script contains no Maven Central publishing step; a fresh fury project builds and runs with the Maven resolver disabled

item
  id dist-9
  track distribution
  horizon long
  status open
  criterion  a scripted third-party verification with public data only passes for the latest release

item
  id dist-10
  track distribution
  horizon near
  status open
  baseline  lib/reliquary (7387 lines) plus anthology.lira, degustation.lira, mandible.lira, xenophile.lira and their tests (2026-09-25)
  criterion  lib/reliquary is absent, no lib/*/src/lira directory remains, and build.mill, etc/ and doc/modules do not mention reliquary

item
  id brd-1
  track breadth
  horizon near
  status open
  criterion  JOSE module ships with tests, SN pages and a topic; round-trips RFC 7515/7516 example vectors

item
  id brd-2
  track breadth
  horizon near
  status open
  criterion  TOML module ships with tests, SN pages and a topic; passes the TOML compliance suite

item
  id brd-3
  track breadth
  horizon mid
  status open
  needs brd-1
  criterion  WebAuthn module ships with tests, SN pages and a topic; scripted ceremony passes against a reference authenticator

item
  id brd-4
  track breadth
  horizon long
  status open
  criterion  QUIC/HTTP-3 module ships with tests, SN pages and a topic; interoperates with reference peers

item
  id brd-5
  track breadth
  horizon long
  status open
  criterion  IMAP module ships with tests, SN pages and a topic; scripted session passes against a reference server

item
  id mod-1
  track modularity
  horizon near
  status done
  done  #1746 (2026-08-08)
  criterion  each dropped edge is verified unused by grep, the deletions compile, and make attest passes

item
  id mod-2
  track modularity
  horizon near
  status done
  done  #1746 (2026-08-08)
  criterion  each listed edge is cut, the new submodules are bundled, and no downstream module regains the severed closure transitively

item
  id mod-3
  track modularity
  horizon near-mid
  status open
  criterion  each split module is bundled and its parent's dependency list contains only edges its remaining code uses

item
  id mod-4
  track modularity
  horizon mid
  status done
  done  #1748, #1756 (2026-08-09)
  criterion  a consumer of one format no longer compiles the others' code, and platform flags reflect the new closures

item
  id mod-5
  track modularity
  horizon mid
  status open
  needs mod-4
  criterion  each algorithm has exactly one implementation, verified by grep for the signature patterns (CRC tables, Huffman builders, varint loops)

item
  id mod-6
  track modularity
  horizon mid-long
  status open
  criterion  no .staged module contains its own copy of the expansion environment, and the benchmark corpus shows performance parity per format

item
  id mod-7
  track modularity
  horizon long
  status open
  criterion  each stretch goal is separately approved before it starts; none is scheduled

item
  id mod-8
  track modularity
  horizon near
  status done
  done  #1758 (2026-08-10)
  criterion  each misdeclaration is fixed, each dead edge is deleted, and every affected component and its dependents compile (make attest, make build)

item
  id mod-9
  track modularity
  horizon near
  status done
  done  #1759 (2026-08-10)
  criterion  exegesis.core's closure contains no compiler-stack module, and the deepest chain through obligatory.json ends at jacinta

item
  id mod-10
  track modularity
  horizon near
  status open
  criterion  parasite.core declares no mercator.core edge and turbulence.core no capricious.core edge; gossamer.core's closure holds no more than 23 components
