_Clavichord_ describes a keypress — an ordinary character, a named editing or function key, or one
of those with modifiers held down — without saying anything about how it is encoded. Terminals and
browsers agree on what a keypress is and disagree only on how it travels, so the model belongs
apart from either.
