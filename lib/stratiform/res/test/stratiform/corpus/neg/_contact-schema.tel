tel 1.0 specification.tel/tels:2.0.0

# A minimal contact schema, used as the schema for sibling
# contact-document-* tests in this directory. Only the bits needed for
# the negative tests are included.

name contact-schema

document
  field name String
  field email String
