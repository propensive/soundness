package serpentine

import rudiments.*
import galilei.*

package hierarchies:
  erased given windows: Hierarchy[Windows.Path, Windows.Link] = ###
  erased given unix: Hierarchy[Unix.Path, Unix.Link] = ###
  erased given flexible: Hierarchy[Path, Link] = ###