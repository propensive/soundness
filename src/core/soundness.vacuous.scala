package soundness

export vacuous.{Default, default, Unset, Optional, UnsetError, or, absent, present, vouch, mask, stdlib, presume, option, assume, lay, layGiven, let, letGiven, compact, optional, puncture, only, Unsafe, Extractor}
